import subprocess

# Equivalent Python code for the given shell commands
subprocess.run(["@echo"], shell=True)
subprocess.run(["title", "ios to desktop"], shell=True)
subprocess.run(["node", "server.js"], shell=True)
